# Shared CI dependency list for macOS jobs (build, sanitize). The
# clang-tidy job in lint.yml additionally installs `llvm` on its own,
# since build/sanitize don't need it and it's the single biggest
# download in the whole pipeline.
brew "ffmpeg"
brew "pkg-config"
brew "cmake"
brew "ccache"
